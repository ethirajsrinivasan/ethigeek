<% content_for :title, @project.title.titleize %>
<!-- <div class="about-banner" style="background-image: url(<%="#{@project.image_url}"%>)"></div> -->
<div class="body_wrapper">
  <div class="row">
  	<div class="large-12 columns">
  		<h1><%= @project.title.titleize %></h1>
      <!-- <section>
        <div class="section-heading"></div>
        <div class="section-body">
          <h3><%= @project.description %>
          </h3>
        </div>
      </section> -->
    </div>
  </div>
  <div class="row">
    <div class="large-12 columns">
      <aside class="about" style="background-color: <%= @project.color %>">
        <%= render "metadata", attribute: @project.duration, title: "Date" %>
        <%= render "metadata", attribute: @project.role, title: "Role" %>
        <%= render "metadata", attribute: @project.tools, title: "Tools " %>
        <%= render "metadata", attribute: @project.tasks, title: "Tasks" %>
        <%= render "metadata", attribute: @project.team, title: "Team" %>
        <%= render "metadata", attribute: @project.platform, title: "Platform" %>
        <%= render "metadata", attribute: @project.collaborators, title: "Collaborators" %>
      </aside>
      <div class="section-content">
        <%= render "section", attribute: @project.summary, title: "Summary" %>
        <%= render "project_links", project: @project %>
        <%= render "section", attribute: @project.learnings, title: "Learnings" %>
        <%= render "section", attribute: @project.appreciation, title: "Appreciation" %>
        <section>
          <div class="section-heading">Data Source</div>
          <div class="section-body">
            Mammogram images are obtained from the following sources
            <ul>
              <li><%= link_to "MIAS","http://www.mammoimage.org/databases/",target: "_blank" %></li>
              <li><%= link_to "DDSM","http://marathon.csee.usf.edu/Mammography/Database.html",target: "_blank" %></li>
            </ul>
          </div>
        </section>
        <section>
          <div class="section-heading">Pre Processing</div>
          <div class="section-body">
          The project is currently at the pre processing stage. Some of the pre processing to be done are
          <ul>
            <li>Remove Mammogram labels</li>
            <li>Identify the biggest blob</li>
          </ul>
          </div>
        </section>
      </div>
    </div>
  </div>
  <br>
  <div class="row">
    <div class="text-center like_project" data-href=<%= "#{portfolios_like_path(title: @project.title)}" %> ><%= image_tag("heart.png",size: 25) %></div>
    <div class="text-center like_count"><%= @project.likes %></div>
  </div>
  <hr>
  <div class="traverse-section" %>
    <div class="row">
      <div class="large-12 columns">
        <% if @previous_project %>
          <div class="left" %>
            <div class="traverse"><%= @previous_project.title.titleize %></div>
            <%= link_to "&larr; Previous project".html_safe, portfolios_path(title: @previous_project.title) %>
          </div>
        <% end %>
        <% if @next_project %>
          <div class="right" %>
            <div class="traverse"><%= @next_project.title.titleize %></div>
            <div class="right"><%= link_to "Next project &rarr; ".html_safe, portfolios_path(title: @next_project.title) %></div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>